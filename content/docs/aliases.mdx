---
title: Aliases
description: Please run /aliases view to see all available aliases for existing character cards.
---

<p className="noteBox"><strong>Note:</strong> You can use both `_` or `space` 
<br></br> **Example:** `dr_ratio` OR `dr ratio` OR `DR RATIO` OR `DR_RATIO` (If any aliases are overlapped/not working, please notice us via Support Server!) </p>
