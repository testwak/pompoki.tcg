---
title: / card colleciton

---

import { TypeTable } from 'fumadocs-ui/components/type-table';

export const types = {
    user: {
        description:
        'Selects a Discord user from dropdown for targeting or interaction.',
        type: 'discord.Member',
        default: 'author',
    },
    sort: {
        description:
        'Other filters',
        type: 'str | choices',
        default: 'all',
    },
    event: {
        description:
        '',
        type: 'str | choices',
        default: 'all',
    },
    series: {
        description:
        '',
        type: 'str | choices',
        default: 'all',
    },
    card_name: {
        description:
        'Which character card would you like to sort? You can also use aliases!',
        type: 'string | choices | aliases',
        default: 'all',
    },
    rarity: {
        description:
        'Card Rarity 1 -> 5',
        type: 'int | choices',
        default: 'all',
    },
    custom_name: {
        description:
        'List of all the card that has the nickname.',
        type: 'string',
        default: 'pass',
    },
    is_locked: {
        description:
        'Filter all unlocked/locked cards. Empty = Display both locked/unlocked cards.',
        type: 'boolean',
        default: 'pass',
    },
    is_albumable: {
        description:
        'Check all available cards you could be added to the album.',
        type: 'boolean',
        default: 'pass',
    },
    has_badge: {
        description:
        'Check all available cards that have the badge.',
        type: 'boolean',
        default: 'pass',
    },
};

<TypeTable type={types} />


| Example                       | Usage |
| --------------------------------- | ----- |
| `/card collection user: @testwak`  | Viewing your or another user's card colleciton.      |


